<h2><%= l(:label_scheduler_resource_new) %></h2>

<%= labelled_form_for @resource, url: project_scheduler_resources_path(@project) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= submit_tag l(:button_create) %>
<% end %>

<% html_title l(:label_scheduler_resource_new) %>
